mixin filter-svg()
    <svg width="6" height="12" viewBox="0 0 6 12" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path d="M3 0L5.59808 4.5H0.401924L3 0Z" fill="#ACC1C0"/>
        <path d="M3 12L5.59808 7.5H0.401924L3 12Z" fill="#ACC1C0"/>
    </svg>

mixin user-item()
    .users__row 
        .users__item.users__item_1
            p 12.10.2022
        .users__item.users__item_2
            p Безумный доктор
        .users__item.users__item_3
            p 48
        .users__item.users__item_4
            p 00:48:20
        .users__item.users__item_5
            p 160 ₽

mixin users()
    include ../tooltip.pug
    .users 
        p.home__title Таблица пользователей
        .users__wrapper 
            .users__inner 
                .users__content 
                    .users__header
                        .users__header-item.users__item_1
                            button(type="button").users__header-btn 
                                +filter-svg
                                p DO
                            +tooltip('DO', 'left')
                        .users__header-item.users__item_2
                            button(type="button").users__header-btn 
                                +filter-svg
                                p Ник игрока
                        .users__header-item.users__item_3
                            button(type="button").users__header-btn
                                +filter-svg
                                p Рейтинг
                            +tooltip('Текущий рейтинг игрока. Стартовый — 50', 'center')
                        .users__header-item.users__item_4
                            button(type="button").users__header-btn 
                                +filter-svg
                                p Время
                            +tooltip('Время', 'center')
                        .users__header-item.users__item_5
                            button(type="button").users__header-btn 
                                +filter-svg
                                p Заработок
                            +tooltip('Заработок', 'right')
                    .users__list 
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item() 
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()
                        +user-item()